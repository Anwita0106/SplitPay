# Root-level .env consumed by docker-compose.yml for the Postgres container
# and to template the backend's DATABASE_URL/REDIS_URL. The backend service
# ALSO reads backend/.env directly for its own settings (SECRET_KEY, etc.) —
# copy both example files.

POSTGRES_USER=splitpay
POSTGRES_PASSWORD=splitpay
POSTGRES_DB=splitpay
